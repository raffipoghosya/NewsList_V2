specVersion: "v1beta"
serviceId: "newslist"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "newslist-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
